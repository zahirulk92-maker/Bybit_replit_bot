import{useEffect,useState}from'react';import{api}from'../services/api';const modes=['ALL','LOCAL_PAPER','BYBIT_DEMO'];export default function Strategy(){const[m,setM]=useState('ALL'),[x,setX]=useState<any[]>([]);useEffect(()=>{api.strategy(m).then(setX)},[m]);return <><h1>Strategy Performance</h1><div className="actions">{modes.map(z=><button className={m===z?'active':''} onClick={()=>setM(z)}>{z}</button>)}</div><div className="strategy-grid">{x.map(s=><div className="card" key={s.id}><h3>{s.name}</h3><strong>{s.status}</strong><p>Total {s.totalTrades} · Win rate {s.winRate}%</p><p>Net PnL {s.netPnl.toFixed(2)} · Avg RR {s.averageRR.toFixed(2)}</p><p>A+ {s.aPlusTrades} · A {s.aTrades}</p></div>)}</div></>}
