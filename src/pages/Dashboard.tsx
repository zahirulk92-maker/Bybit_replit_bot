import {useEffect,useState} from'react';import{api}from'../services/api';
export default function Dashboard(){const[s,setS]=useState<any>();useEffect(()=>{api.status().then(setS).catch(()=>{})},[]);return <><h1>Trading Operations Dashboard</h1><p className="muted">Real Bybit public market data. Execution remains disabled unless explicitly configured.</p><div className="grid">{[['Execution Mode',s?.executionMode||'DISABLED'],['Scanner',s?.scanner?.enabled?'ON':'OFF'],['Active Signals',s?.activeSignals??0],['Active Trades',s?.activeTrades??0],['Pause New Entries',s?.pauseNewEntries?'YES':'NO'],['Emergency Stop',s?.emergencyStop?'ACTIVE':'CLEAR']].map(x=><div className="card" key={x[0]}><small>{x[0]}</small><strong>{x[1]}</strong></div>)}</div></>}
