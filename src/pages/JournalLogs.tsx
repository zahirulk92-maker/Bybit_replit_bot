import{useEffect,useState}from'react';import{api}from'../services/api';export default function JournalLogs(){const[j,setJ]=useState<any[]>([]),[l,setL]=useState<any[]>([]);useEffect(()=>{api.journal().then(setJ);api.logs().then(setL)},[]);return <><h1>Journal / Logs</h1><h2>Completed Trades</h2>{j.length?<pre>{JSON.stringify(j,null,2)}</pre>:<div className="empty">No completed trades.</div>}<h2>System Logs</h2><pre>{JSON.stringify(l.slice(-50),null,2)}</pre></>}
